var productId = $(this).attr('id');
console.log(productId);
$(productId).click(
    function(){
        var descriptionId = $(this).attr('id');
        $('#product-description-' + descriptionId).toggleClass('hidden');
        //console.log('.right-list #' + listId);
});
